# 测试：数据量不对（分类一 → 场景 1）
# 目标：验证 skill 会处理数据量相关描述

## prompt

> 数仓和业务库对不上，今天少了大概 3000 条数据，都是凌晨那段时间缺的。

## expected

Agent 应进入"一、数据问题"分支，检查时区偏差，给出增量窗口加缓冲的方案。

## trigger_check

- 应触发：✅ 是（"对不上""少了"+"凌晨+缺"）
