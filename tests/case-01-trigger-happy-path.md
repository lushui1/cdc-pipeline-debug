# 测试用例 01：显式触发 — 增量数据对不上

## prompt

> 我数仓今天的增量数据对不上业务库，TMS显示12000票，我查出来只有8500票，差的3500票去哪了？

## expected

Agent 应该：
1. 识别出这是增量同步数据量对不上的问题
2. 询问：同步工具、源库类型、时区配置
3. 按步骤排查：先对账按小时对比 → 检查时区 → 检查物理删除
4. 给出时间窗口加缓冲的方案

## trigger_check

- 应触发：✅ 是（包含"数据对不上""增量"等关键词）
